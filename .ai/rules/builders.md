---
paths:
  - 'app/Builders/**'
---

# Builders

## Custom Eloquent builders
Put reusable model query operations on typed custom Eloquent Builder classes and bind them to models with #[UseEloquentBuilder]; do not add local model scopes.
