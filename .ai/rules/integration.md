---
paths:
  - 'tests/Integration/**'
---

# Integration

## Refresh databases in application test suites
Use RefreshDatabase for Integration tests; keep database reset configuration centralized in tests/Pest.php.
